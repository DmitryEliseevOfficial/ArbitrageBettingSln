<Window x:Class="EditMaps.View.EditBaseView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EditMaps.View"
        mc:Ignorable="d"
        Title="EditBaseView" Height="292.105" Width="609.022">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50"></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>
        <Slider Maximum="{Binding Maximum}" Value="{Binding CurentId}"></Slider>

        <Grid Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="150"></ColumnDefinition>
                </Grid.ColumnDefinitions>

                <ListBox ItemsSource="{Binding CurrentList}">

                </ListBox>

                <StackPanel  Grid.Column="1">
                    <Button>123</Button>
                </StackPanel>
                
            </Grid>

            
        </Grid>
    </Grid>
</Window>
